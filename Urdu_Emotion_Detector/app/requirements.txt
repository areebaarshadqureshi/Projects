# Streamlit app requirements for HuggingFace Spaces deployment
# Lighter dependencies - CPU-only torch, no training libraries

streamlit>=1.28.0
transformers>=4.30.0
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
sentencepiece>=0.1.99
protobuf>=3.20.0
